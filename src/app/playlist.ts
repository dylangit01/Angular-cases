export class PlayList {
      id: number;
      artist: string;
      track: string;
      listened: boolean;
      favourite: boolean;
      songStyle: string;
      photoPath?: string;
      password: string;
      confirmPassword: string;
}
